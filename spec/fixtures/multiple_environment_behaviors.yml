handlers:
  - environments:
    - environment: production
      behavior: ignore
    - environment: development
      behavior: log
    - environment: test
      behaviors:
        - raise
        - log
